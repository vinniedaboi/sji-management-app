import Link from "next/link";
import { addDays, format } from "date-fns";
import { CalendarDays, CheckCircle2, ChevronLeft, ChevronRight, ClipboardCheck, FileText, Plus, RotateCcw, Send, UserRoundX, Users } from "lucide-react";
import { applyForCover, assignCoverSlot, cancelAbsence, createCoverSlot, publishCoverNotice, reopenCoverSlot, reportAbsence, withdrawCoverApplication } from "@/app/actions";
import { Badge, EmptyState, PageHeader } from "@/components/app-shell";
import { requireUser } from "@/lib/auth";
import { absenceTypes, absencesForDate, availableCoverStaff, coverApplicationsForDate, coverBoard, manageableAbsences, schoolPeriods } from "@/lib/cover";
import { db, rows } from "@/lib/db";
import { schoolDate } from "@/lib/format";
import { isAdmin } from "@/lib/types";

const datePattern=/^\d{4}-\d{2}-\d{2}$/;
const errorMessages:Record<string,string>={dates:"Choose a valid absence date range.",overlap:"That colleague already has an absence recorded for these dates.",slot:"Check the period and lesson details.","duplicate-slot":"A cover requirement already exists for that absence and period.",unavailable:"That cover slot is no longer available.",conflict:"That colleague already has an absence, assignment or application in this period.",assignee:"Choose an available teaching colleague.","no-slots":"Add at least one cover requirement before publishing the notice."};

export default async function CoverPage({searchParams}:{searchParams:Promise<{date?:string;period?:string;status?:string;report?:string;add?:string;error?:string}>}){
  const user=await requireUser();const params=await searchParams;const date=datePattern.test(params.date??"")?params.date!:schoolDate();const period=schoolPeriods.includes(Number(params.period) as (typeof schoolPeriods)[number])?Number(params.period):0;const status=params.status==="open"||params.status==="assigned"?params.status:"all";const manager=user.role!=="teacher";const unrestricted=isAdmin(user);
  const [slots,absences,applications,coverStaff,slotAbsences,absenceStaff]=await Promise.all([
    coverBoard(date,user.id,period,status),
    absencesForDate(date),
    manager?coverApplicationsForDate(date):Promise.resolve([]),
    manager?availableCoverStaff(date):Promise.resolve([]),
    manager?manageableAbsences(date,user.departmentId,unrestricted):Promise.resolve([]),
    manager?db().execute({sql:`SELECT u.id,u.full_name as fullName,u.job_title as jobTitle FROM users u WHERE u.active=1 AND u.role IN ('teacher','department_head') AND (?=1 OR u.department_id=?) ORDER BY u.full_name`,args:[unrestricted?1:0,user.departmentId]}).then(result=>rows<{id:string;fullName:string;jobTitle:string}>(result)):Promise.resolve([]),
  ]);
  const previous=format(addDays(new Date(`${date}T12:00:00`),-1),"yyyy-MM-dd"),next=format(addDays(new Date(`${date}T12:00:00`),1),"yyyy-MM-dd");const assigned=slots.filter(slot=>slot.status==="assigned").length,open=slots.filter(slot=>slot.status==="open").length,myAssignments=slots.filter(slot=>slot.assignedUserId===user.id).length;const applicationsBySlot=Map.groupBy(applications,application=>application.coverSlotId);
  return <>
    <PageHeader eyebrow="DAILY OPERATIONS" title="Cover" description="See who is away, coordinate every period, and volunteer for available cover." actions={<div className="cover-header-actions"><Link className="button button-secondary" href={`/cover?date=${date}&report=1`}><UserRoundX size={16}/>Report absence</Link>{manager&&<Link className="button button-primary" href={`/cover?date=${date}&add=1`}><Plus size={16}/>Add cover need</Link>}</div>}/>
    {params.error&&<div className="form-error">{errorMessages[params.error]??"That cover action could not be completed."}</div>}

    {params.report&&<section className="surface form-panel cover-form-panel"><h2><UserRoundX size={18}/>Report an absence</h2><form action={reportAbsence} className="form-grid">{manager?<label className="span-2">Absent colleague<select name="staffId" required>{absenceStaff.map(person=><option value={person.id} key={person.id}>{person.fullName} — {person.jobTitle}</option>)}</select></label>:<div className="span-2 notice-inline"><Users size={17}/><p>This absence will be recorded for <strong>{user.fullName}</strong>.</p></div>}<label>First day<input type="date" name="startDate" defaultValue={date} required/></label><label>Last day<input type="date" name="endDate" defaultValue={date} required/></label><label>Absence type<select name="absenceType">{absenceTypes.map(type=><option key={type}>{type}</option>)}</select></label><label>Operational note<input name="notes" maxLength={500} placeholder="Information for the cover coordinator"/></label><div className="span-2 form-actions"><Link className="button button-secondary" href={`/cover?date=${date}`}>Cancel</Link><button className="button button-primary">Record absence</button></div></form></section>}

    {manager&&params.add&&<section className="surface form-panel cover-form-panel"><h2><ClipboardCheck size={18}/>Add a cover requirement</h2>{slotAbsences.length?<form action={createCoverSlot} className="form-grid"><label className="span-2">Absent colleague<select name="absenceId" required>{slotAbsences.map(absence=><option value={absence.id} key={absence.id}>{absence.staffName}</option>)}</select></label><input type="hidden" name="coverDate" value={date}/><label>Period<select name="period">{schoolPeriods.map(item=><option key={item} value={item}>Period {item}</option>)}</select></label><label>Class or group<input name="className" required placeholder="e.g. 8A"/></label><label>Subject<input name="subject" required placeholder="e.g. Mathematics"/></label><label>Room<input name="room" placeholder="e.g. C3-06"/></label><label className="span-2">Cover instructions<textarea name="instructions" rows={3} maxLength={1000} placeholder="Work set, register notes, equipment or safeguarding information"/></label><div className="span-2 form-actions"><Link className="button button-secondary" href={`/cover?date=${date}`}>Cancel</Link><button className="button button-primary">Add to cover board</button></div></form>:<div className="notice-inline notice-warning"><UserRoundX size={17}/><p>Record an absence for {date} before adding cover requirements.</p></div>}</section>}

    <section className="surface cover-controls"><div className="cover-date-nav"><Link className="icon-button" href={`/cover?date=${previous}`} aria-label="Previous day"><ChevronLeft size={18}/></Link><form><label><CalendarDays size={16}/><span className="sr-only">Cover date</span><input type="date" name="date" defaultValue={date}/></label><input type="hidden" name="period" value={period||""}/><input type="hidden" name="status" value={status}/><button className="button button-secondary">Go</button></form><Link className="icon-button" href={`/cover?date=${next}`} aria-label="Next day"><ChevronRight size={18}/></Link><Link className="button button-secondary" href={`/cover?date=${schoolDate()}`}>Today</Link></div><form className="cover-filter-form"><label>Period<select name="period" defaultValue={period}><option value="0">All periods</option>{schoolPeriods.map(item=><option key={item} value={item}>Period {item}</option>)}</select></label><label>Status<select name="status" defaultValue={status}><option value="all">All cover</option><option value="open">Open only</option><option value="assigned">Assigned only</option></select></label><input type="hidden" name="date" value={date}/><button className="button button-secondary">Filter</button></form></section>

    <section className="cover-stats" aria-label="Cover summary"><article className="surface"><span><UserRoundX size={18}/></span><div><strong>{absences.length}</strong><small>Absent</small></div></article><article className="surface"><span className={open?"stat-warning":"stat-success"}><ClipboardCheck size={18}/></span><div><strong>{open}</strong><small>Cover needed</small></div></article><article className="surface"><span className="stat-success"><CheckCircle2 size={18}/></span><div><strong>{assigned}</strong><small>Assigned</small></div></article><article className="surface"><span><Users size={18}/></span><div><strong>{myAssignments}</strong><small>Assigned to me</small></div></article></section>

    <section className="surface absent-panel"><div className="surface-header"><h2><UserRoundX size={17}/>Who is absent</h2><Badge tone={absences.length?"important":"success"}>{absences.length||"None"}</Badge></div>{absences.length?<div className="absence-list">{absences.map(absence=>{const canManageAbsence=absence.staffId===user.id||unrestricted||user.role==="department_head"&&absence.departmentId===user.departmentId;return <article key={absence.id}><span className="avatar">{absence.staffName.split(/\s+/).map(part=>part[0]).join("").slice(0,2)}</span><div><strong>{absence.staffName}</strong><small>{absence.jobTitle} · {absence.departmentName??"School"}</small>{canManageAbsence&&absence.notes&&<span className="absence-note">{absence.notes}</span>}</div><Badge>{canManageAbsence?absence.absenceType:"Away"}</Badge>{canManageAbsence&&<form action={cancelAbsence}><input type="hidden" name="absenceId" value={absence.id}/><button className="text-link" type="submit">Cancel absence</button></form>}</article>})}</div>:<p className="panel-empty">No confirmed staff absences for this date.</p>}</section>

    <div className="cover-board-heading"><div><p className="eyebrow">PERIOD-BY-PERIOD</p><h2>Cover board</h2></div>{manager&&slots.length>0&&<form action={publishCoverNotice}><input type="hidden" name="coverDate" value={date}/><button className="button button-secondary"><FileText size={15}/>Publish as notice</button></form>}</div>
    <section className="surface cover-table-wrap">{slots.length?<table className="cover-table"><thead><tr><th>Period</th><th>Absent</th><th>Lesson</th><th>Room</th><th>Cover</th><th>Status & actions</th></tr></thead><tbody>{slots.map(slot=>{const canManage=unrestricted||user.role==="department_head"&&slot.departmentId===user.departmentId;const slotApplications=applicationsBySlot.get(slot.id)??[];return <tr key={slot.id}><td data-label="Period"><span className="period-pill">{slot.period}</span></td><td data-label="Absent"><strong>{slot.absentStaffName}</strong><small>{slot.departmentName}</small></td><td data-label="Lesson"><strong>{slot.className} · {slot.subject}</strong>{slot.instructions&&<small>{slot.instructions}</small>}</td><td data-label="Room">{slot.room||"—"}</td><td data-label="Cover">{slot.assignedUserName?<><strong>{slot.assignedUserName}</strong>{slot.assignedUserId===user.id&&<Badge tone="success">You</Badge>}</>:<span className="cover-needed">Cover needed</span>}</td><td data-label="Status & actions"><div className="cover-actions"><Badge tone={slot.status==="assigned"?"success":"important"}>{slot.status}</Badge>{canManage?(slot.status==="assigned"?<form action={reopenCoverSlot}><input type="hidden" name="slotId" value={slot.id}/><button className="text-link"><RotateCcw size={13}/>Reopen</button></form>:<details className="cover-assignment"><summary>{slot.pendingApplicationCount?`${slot.pendingApplicationCount} applications`:"Assign cover"}</summary>{slotApplications.length>0&&<div className="application-list">{slotApplications.map(application=><article key={application.id}><div><strong>{application.applicantName}</strong><small>{application.applicantJobTitle}</small>{application.note&&<p>{application.note}</p>}</div><form action={assignCoverSlot}><input type="hidden" name="slotId" value={slot.id}/><input type="hidden" name="coverUserId" value={application.applicantId}/><button className="button button-primary">Approve</button></form></article>)}</div>}<form action={assignCoverSlot} className="direct-assign"><input type="hidden" name="slotId" value={slot.id}/><label><span className="sr-only">Assign a colleague</span><select name="coverUserId" required defaultValue=""><option value="" disabled>Select colleague…</option>{coverStaff.filter(person=>person.id!==slot.absentStaffId).map(person=><option value={person.id} key={person.id}>{person.fullName}</option>)}</select></label><button className="button button-secondary">Assign</button></form></details>):slot.status==="open"&&slot.absentStaffId!==user.id?(slot.myApplicationStatus==="pending"?<div className="application-pending"><span>Application pending</span><form action={withdrawCoverApplication}><input type="hidden" name="applicationId" value={slot.myApplicationId??""}/><button className="text-link">Withdraw</button></form></div>:<details className="cover-apply"><summary>Apply to cover</summary><form action={applyForCover}><input type="hidden" name="slotId" value={slot.id}/><label>Note (optional)<input name="note" maxLength={300} placeholder="Relevant experience or context"/></label><label className="check"><input type="checkbox" name="availabilityConfirmed" required/>I confirm I am free this period</label><button className="button button-primary"><Send size={14}/>Submit application</button></form></details>):null}</div></td></tr>})}</tbody></table>:<EmptyState title="No cover requirements" description="There are no cover slots matching this date and filter."/>}</section>
  </>;
}
